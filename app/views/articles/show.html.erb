<h1>
  <%= @article.title %>
  <small>
        <%= link_to edit_article_path(@article), class: "text-decoration-none" do %>
          <i class="fa-solid fa-pen-to-square"></i>

        <% end %>
        <%= link_to article_path(@article), data: { turbo_method: :delete, turbo_confirm: 'Are you sure?' } do %>
          <i class="fa-regular fa-trash-can"></i>
        <% end %>
      </small>
  <hr>
</h1>
<div>
  <p><%= @article.content.truncate_words(10) %></p>
</div>
<div>
  <%= @article.content %>
</div>
<%= link_to articles_path %>
